const SUPABASE_URL=window.CSATANGOLO_SUPABASE_URL;const SUPABASE_ANON_KEY = window.CSATANGOLO_SUPABASE_ANON_KEY;const client=supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY);const BUCKET="forum-assets";function slug(s){return String(s||"file").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"file"}document.getElementById("mediaUploadForm")?.addEventListener("submit",async e=>{e.preventDefault();const form=e.target,msg=document.getElementById("uploadMessage"),file=form.elements.file.files[0];if(!file)return;msg.textContent="Feltöltés folyamatban...";try{const folder=file.type.startsWith("video/")?"visitor-videos":"visitor-photos";const ext=file.name.split(".").pop();const path=`${folder}/${Date.now()}-${slug(file.name)}.${ext}`;const {error:up}=await client.storage.from(BUCKET).upload(path,file,{upsert:false});if(up)throw up;const {data}=client.storage.from(BUCKET).getPublicUrl(path);const {error:db}=await client.from("media_uploads").insert({uploader_name:form.elements.uploader_name.value.trim(),note:form.elements.note.value.trim(),file_url:data.publicUrl,file_type:file.type,is_approved:false});if(db)throw db;msg.className="form-message success";msg.textContent="Köszönjük! A feltöltés megérkezett a szervezőkhöz.";form.reset();}catch(err){console.error(err);msg.className="form-message error";msg.textContent="Nem sikerült feltölteni. Ellenőrizd, hogy lefutott-e a Master SQL.";}});